{
  "name": "homebridge-tibber-price-next",
  "displayName": "Homebridge Tibber Price Next",
  "version": "0.1.0",
  "description": "Neuaufbau eines Homebridge-Plugins fuer Tibber-Preise mit flexiblen Zeitslots",
  "license": "MIT",
  "type": "commonjs",
  "main": "dist/index.js",
  "files": [
    "dist",
    "src",
    "config.schema.json",
    "README.md"
  ],
  "engines": {
    "node": ">=20",
    "homebridge": ">=1.8.0"
  },
  "scripts": {
    "build": "node scripts/prepare.mjs",
    "prepare": "node scripts/prepare.mjs",
    "test": "node --test test/*.test.cjs"
  },
  "keywords": [
    "homebridge-plugin",
    "tibber",
    "energy",
    "dynamic-pricing"
  ],
  "dependencies": {
    "homebridge": "^1.8.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0"
  }
}
